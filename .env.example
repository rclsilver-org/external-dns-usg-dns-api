# Configuration# Server Configuration
# Webhook API listening port (default: 8888)
SERVER_PORT=8888

# Health check listening port (default: 8080)
HEALTH_PORT=8080

# Options
# Dry run mode: no actual changes will be made (default: false)
DRY_RUN=false
ernal-dns-usg-dns-api

# USG DNS API Configuration
# Your usg-dns-api URL (e.g., http://192.168.1.1:8080)
USG_DNS_URL=http://192.168.1.1:8080

# Authentication token for usg-dns-api
# Generated with: usg-dns-api generate-token
USG_DNS_TOKEN=your-token-here

# Domain Filter (optional)
# List of domains to manage, comma-separated
# If empty, all domains will be managed
DOMAIN_FILTER=example.com,local.domain

# Server Configuration
# Webhook listening port (default: 8888)
SERVER_PORT=8888

# Options
# Dry-run mode: if true, changes will not be applied (default: false)
DRY_RUN=false
